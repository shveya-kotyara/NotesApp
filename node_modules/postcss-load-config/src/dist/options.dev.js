'use strict';

var req = require('import-cwd');
/**
 * Load Options
 *
 * @private
 * @method options
 *
 * @param  {Object} config  PostCSS Config
 *
 * @return {Object} options PostCSS Options
 */


var options = function options(config, file) {
  if (config.parser && typeof config.parser === 'string') {
    try {
      config.parser = req(config.parser);
    } catch (err) {
      throw new Error("Loading PostCSS Parser failed: ".concat(err.message, "\n\n(@").concat(file, ")"));
    }
  }

  if (config.syntax && typeof config.syntax === 'string') {
    try {
      config.syntax = req(config.syntax);
    } catch (err) {
      throw new Error("Loading PostCSS Syntax failed: ".concat(err.message, "\n\n(@").concat(file, ")"));
    }
  }

  if (config.stringifier && typeof config.stringifier === 'string') {
    try {
      config.stringifier = req(config.stringifier);
    } catch (err) {
      throw new Error("Loading PostCSS Stringifier failed: ".concat(err.message, "\n\n(@").concat(file, ")"));
    }
  }

  if (config.plugins) {
    delete config.plugins;
  }

  return config;
};

module.exports = options;