'use strict';

function getVersions() {
  return "webpack-dev-server ".concat(require('../../package.json').version, "\n") + "webpack ".concat(require('webpack/package.json').version);
}

module.exports = getVersions;