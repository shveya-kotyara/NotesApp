'use strict';

var resolveFrom = require('resolve-from');

module.exports = function (moduleId) {
  return resolveFrom(process.cwd(), moduleId);
};

module.exports.silent = function (moduleId) {
  return resolveFrom.silent(process.cwd(), moduleId);
};