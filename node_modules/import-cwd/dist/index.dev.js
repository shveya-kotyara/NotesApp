'use strict';

var importFrom = require('import-from');

module.exports = function (moduleId) {
  return importFrom(process.cwd(), moduleId);
};

module.exports.silent = function (moduleId) {
  return importFrom.silent(process.cwd(), moduleId);
};