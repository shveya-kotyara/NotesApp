'use strict';

var TYPE = 'module-not-found';

function isModuleNotFoundError(e) {
  var webpackError = e.webpackError || {};
  return webpackError.dependencies && webpackError.dependencies.length > 0 && e.name === 'ModuleNotFoundError' && e.message.indexOf('Module not found') === 0;
}

function transform(error) {
  var webpackError = error.webpackError;

  if (isModuleNotFoundError(error)) {
    var dependency = webpackError.dependencies[0];

    var _module = dependency.request || dependency.options.request;

    return Object.assign({}, error, {
      message: "Module not found ".concat(_module),
      type: TYPE,
      severity: 900,
      module: _module,
      name: 'Module not found'
    });
  }

  return error;
}

module.exports = transform;