"use strict";

var transformOn = function transformOn(obj) {
  var result = {};
  Object.keys(obj).forEach(function (evt) {
    result["on".concat(evt[0].toUpperCase()).concat(evt.substr(1))] = obj[evt];
  });
  return result;
};

module.exports = transformOn;