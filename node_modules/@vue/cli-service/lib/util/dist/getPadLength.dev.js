"use strict";

module.exports = function getPadLength(obj) {
  var longest = 10;

  for (var name in obj) {
    if (name.length + 1 > longest) {
      longest = name.length + 1;
    }
  }

  return longest;
};