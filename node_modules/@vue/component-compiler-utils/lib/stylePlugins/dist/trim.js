"use strict";
exports.__esModule = true;
var postcss = require("postcss");
exports["default"] = postcss.plugin('trim', function () { return function (css) {
    css.walk(function (_a) {
        var type = _a.type, raws = _a.raws;
        if (type === 'rule' || type === 'atrule') {
            if (raws.before)
                raws.before = '\n';
            if (raws.after)
                raws.after = '\n';
        }
    });
}; });
