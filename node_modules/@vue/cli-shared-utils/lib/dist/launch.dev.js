"use strict";

var launch = require('launch-editor');

exports.launch = function () {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }

  var file = args[0];
  console.log("Opening ".concat(file, "..."));
  var cb = args[args.length - 1];

  if (typeof cb !== 'function') {
    cb = null;
  }

  launch.apply(void 0, args.concat([function (fileName, errorMessage) {
    console.error("Unable to open '".concat(fileName, "'"), errorMessage);
    console.log("Try setting the EDITOR env variable. More info: https://github.com/yyx990803/launch-editor");
    if (cb) cb(fileName, errorMessage);
  }]));
};