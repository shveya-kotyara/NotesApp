'use strict';

var spinners = Object.assign({}, require('./spinners.json'));
var spinnersList = Object.keys(spinners);
Object.defineProperty(spinners, 'random', {
  get: function get() {
    var randomIndex = Math.floor(Math.random() * spinnersList.length);
    var spinnerName = spinnersList[randomIndex];
    return spinners[spinnerName];
  }
});
module.exports = spinners; // TODO: Remove this for the next major release

module.exports["default"] = spinners;